require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe ApplicationController do
  describe "#select_land_point" do
    it "redirects to the portfolio page if user is an owner"
    
    it "redirects to the home page if user is not an owner"
  end
end